math.ceil()

isqrt() 开根号向下取整

floor()向下取整

转变大小写的方法可以使用位运算实现。对于一个字母，小写形式与大写形式的 ASCII 码之差为 323232。
因此，我们可以通过将该字母的 ASCII 码与 323232 进行异或运算来实现大小写转换。
chr(ord(letter) ^ 32)

bisect()和bisect_right()等同
bisect.bisect和bisect.bisect_right返回大于x的第一个下标，bisect.bisect_left返回大于等于x的第一个下标
index1 = bisect(ls, x)   #第1个参数是列表，第2个参数是要查找的数，返回值为索引
index2 = bisect_left(ls, x)
index3 = bisec_right(ls, x)

from itertools import accumulate

str.find(str)

